-- SQL template to create the project database. Replace placeholders as needed.
CREATE DATABASE IF NOT EXISTS `motorpartshub`;
-- If you want to create a dedicated user, uncomment and edit the following lines:
-- CREATE USER 'mph_user'@'localhost' IDENTIFIED BY 'strong_password_here';
-- GRANT ALL PRIVILEGES ON `motorpartshub`.* TO 'mph_user'@'localhost';
-- FLUSH PRIVILEGES;

-- After running this, update your .env with the chosen user/password.
