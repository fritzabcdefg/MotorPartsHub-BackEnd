// Item detail base script
(function () {
    function showDetails(item) {
        // Render item details on the page (placeholder)
        console.log('Show item', item);
    }

    window.Item = { showDetails };
})();
